'use strict';
function hello()
{
    console.clear;
    let array=[];
    let N=prompt('Enter no. of elementes : ');
    for(let i=0; i<N;i++);
    {
        let a=prompt('Enter elements in array : ');
        
    }
    console.log(array);
}
